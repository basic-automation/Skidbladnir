{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The permissions the Skidbladnir window needs. Kept minimal on purpose: the app writes files the user chose and nothing else.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
